# netlify.toml
# Configures static publishing from public/ and serverless functions in api/

[build]
  publish = "public"

[functions]
  directory = "api"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
