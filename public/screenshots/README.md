Put committed website screenshots in this folder.

Use the member site's Unique ID/slug as the filename:

- `my-blog.png` for a site with slug `my-blog`
- served on the site as `/screenshots/my-blog.png`

Only verified sites with a matching PNG appear in the homepage Member Gallery.
